<div class="container mx-auto px-4 py-8">
  <div class="max-w-5xl mx-auto mb-6 flex items-center">
    <a href="<%= supplier_path(@supplier) %>" class="inline-flex items-center px-4 py-2 rounded-lg text-white font-medium transition-all shadow-sm hover:shadow-md" style="background-color: #38bdf8;">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
        <path fill-rule="evenodd" d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z" clip-rule="evenodd" />
      </svg>
      Volver a <%= @supplier.name %>
    </a>
  </div>

  <div class="max-w-5xl mx-auto bg-white rounded-xl shadow-md overflow-hidden mb-10">
    <div class="flex flex-col md:flex-row">
      <div class="md:w-1/2 p-6 flex items-center justify-center bg-gray-50">
        <% if @product.image_url.present? %>
          <img src="<%= @product.image_url %>" alt="<%= @product.name %>" class="max-w-full max-h-96 object-contain">
        <% else %>
          <div class="w-full h-64 bg-gray-200 rounded-lg flex items-center justify-center">
            <span class="text-gray-500 font-medium">Imagen no disponible</span>
          </div>
        <% end %>
      </div>
      
      <div class="md:w-1/2 p-6">
        <div class="border-l-4 pl-4 mb-6 border-primary-400">
          <h1 class="text-2xl font-bold mb-2" style="color: #38bdf8;"><%= @product.name %></h1>
          <p class="text-gray-500">SKU: <%= @product.sku %></p>
        </div>
        
        <div class="mb-6">
          <h3 class="text-lg font-medium mb-2" style="color: #38bdf8;">Descripción</h3>
          <p class="text-gray-700"><%= @product.description %></p>
        </div>
        
        <div class="flex justify-between items-center mb-6">
          <div>
            <h3 class="text-lg font-medium mb-1" style="color: #38bdf8;">Categoría</h3>
            <span class="inline-block px-3 py-1 bg-gray-100 text-gray-700 rounded-full text-sm">
              <%= @product.category %>
            </span>
          </div>
          
          <div class="text-right">
            <h3 class="text-lg font-medium mb-1" style="color: #38bdf8;">Precio</h3>
            <span class="text-2xl font-bold" style="color: #38bdf8;">L. <%= number_with_precision(@product.price, precision: 2) %></span>
          </div>
        </div>
        
        <div class="border-t border-gray-200 pt-6">
          <h3 class="text-lg font-medium mb-4" style="color: #38bdf8;">Información del Proveedor</h3>
          
          <div class="flex items-center mb-4">
            <% if @supplier.logo_url.present? %>
              <img src="<%= @supplier.logo_url %>" alt="<%= @supplier.name %>" class="w-12 h-12 object-contain mr-4 rounded-full">
            <% else %>
              <div class="w-12 h-12 bg-gray-200 rounded-full flex items-center justify-center mr-4">
                <span class="text-gray-500 font-bold"><%= @supplier.name.first %></span>
              </div>
            <% end %>
            <div>
              <h4 class="font-medium" style="color: #38bdf8;"><%= @supplier.name %></h4>
              <p class="text-gray-600 text-sm"><%= @supplier.phone %></p>
            </div>
          </div>
          
          <a href="<%= supplier_path(@supplier) %>" class="inline-flex items-center px-5 py-2.5 text-white font-medium rounded-full transition-all shadow-md hover:shadow-lg transform hover:scale-105" style="background-color: #38bdf8;">
            <span>Ver todos los productos</span>
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M10.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L12.586 11H5a1 1 0 110-2h7.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" />
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</div>
