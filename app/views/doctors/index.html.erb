<div class="container mx-auto px-4 py-8">
  <header class="text-center mb-12">
    <h1 class="text-4xl font-bold text-primary-700 dark:text-sky-400 mb-4">Directorio de Médicos</h1>
    <p class="text-xl text-gray-600 dark:text-slate-400">Encuentra el especialista que necesitas en Honduras</p>
  </header>

  <div class="max-w-5xl mx-auto mb-8">
    <div class="bg-white dark:bg-slate-800 rounded-full shadow-md dark:shadow-slate-900/50 p-2 transition-all hover:shadow-lg border border-transparent dark:border-slate-700">
      <form action="/doctors" method="get" class="flex items-center">
        <div class="flex-grow pl-4">
          <input type="text" name="q" value="<%= params[:q] %>" placeholder="Busca por nombre, especialidad, ciudad o departamento" class="w-full py-3 px-2 text-gray-700 dark:text-slate-200 focus:outline-none bg-transparent dark:placeholder-slate-400">
        </div>
        <button type="submit" class="ml-2 py-3 px-6 rounded-full text-white font-medium transition-all hover:shadow-md transform hover:scale-105 bg-sky-500 hover:bg-sky-600">
          Buscar
        </button>
      </form>
    </div>
  </div>

  <div class="max-w-5xl mx-auto mb-8 flex flex-col md:flex-row gap-8">
    <!-- Filters sidebar -->
    <div class="md:w-1/4">
      <%= render 'shared/doctor_filters' %>
    </div>

    <!-- Main content -->
    <div class="md:w-3/4">
      <div class="mb-6">
        <h2 class="text-2xl font-bold text-primary-600 dark:text-sky-400">Resultados</h2>
      </div>
      <turbo-frame id="doctors-list">

      <div class="grid md:grid-cols-1 gap-8">
    <% @doctors.each do |doctor| %>
      <div class="bg-white dark:bg-slate-800 rounded-xl shadow-md dark:shadow-slate-900/50 overflow-hidden border-l-8 transition-all hover:shadow-lg border-sky-300 dark:border-sky-500">
        <div class="flex flex-col md:flex-row">
          <div class="md:w-1/4 p-4">
            <% if doctor.image_url.present? %>
              <img src="<%= doctor.image_url %>" alt="<%= doctor.display_name %>" class="w-full h-auto rounded-lg object-cover" />
            <% else %>
              <div class="w-full h-full bg-gray-200 dark:bg-slate-700 rounded-lg flex items-center justify-center">
                <span class="text-gray-500 dark:text-slate-400">Sin imagen</span>
              </div>
            <% end %>
          </div>
          <div class="md:w-3/4 p-4">
            <div class="flex flex-col md:flex-row md:justify-between md:items-start">
              <div>
                <h2 class="text-xl font-semibold mb-2 text-sky-600 dark:text-sky-400"><%= doctor.display_name %></h2>
                <p class="text-gray-700 dark:text-slate-300 mb-1">Especialista en <%= doctor.specialty&.name %></p>
                <% if doctor.subspecialty.present? %>
                  <p class="text-gray-700 dark:text-slate-300 mb-1">Subespecialidad: <%= doctor.subspecialty.name %></p>
                <% end %>
                <p class="text-gray-500 dark:text-slate-400 text-sm mb-3"><%= doctor.odontologia if doctor.respond_to?(:odontologia) %></p>
              </div>

              <% if doctor.establishments.any? %>
                <div class="mb-3 md:text-right">
                  <h3 class="text-lg font-medium text-sky-600 dark:text-sky-400"><%= doctor.establishments.first.name %></h3>
                  <p class="text-gray-500 dark:text-slate-400 text-sm"><%= doctor.establishments.first.address %></p>
                </div>
              <% else %>
                <div class="mb-3 md:text-right">
                  <p class="text-gray-500 dark:text-slate-400 text-sm"><%= doctor.city&.name %>, <%= doctor.department&.name %></p>
                </div>
              <% end %>
            </div>

            <div class="mt-4 text-right">
              <a href="<%= doctor_path(doctor) %>" data-turbo-frame="_top" class="inline-flex items-center px-5 py-2.5 text-white font-medium rounded-full transition-all shadow-md hover:shadow-lg transform hover:scale-105 bg-sky-500 hover:bg-sky-600">
                <span class="text-sm">VER PERFIL COMPLETO</span>
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L12.586 11H5a1 1 0 110-2h7.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" />
                </svg>
              </a>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Pagination controls -->
    <% if @total_pages > 1 %>
      <div class="mt-10 mb-6 flex justify-center">
        <nav class="shadow-sm rounded-lg" aria-label="Pagination">
          <%= pagination_links(@page, @total_pages, { q: params[:q], specialty: params[:specialty], service: params[:service] }) %>
        </nav>
      </div>
    <% end %>
      </div>
      </turbo-frame>
    </div>
  </div>
</div>
