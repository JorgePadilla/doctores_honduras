<div class="max-w-6xl mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold text-primary-700 mb-6">Configuración</h1>
  
  <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
    <!-- Sidebar Navigation -->
    <div class="bg-white rounded-lg shadow-md p-4">
      <nav class="space-y-1">
        <a href="<%= settings_path %>" class="block px-3 py-2 rounded-md text-gray-700 hover:bg-gray-50 hover:text-gray-900">
          <i class="fas fa-cog mr-2"></i>General
        </a>
        <a href="<%= settings_account_path %>" class="block px-3 py-2 rounded-md text-gray-700 hover:bg-gray-50 hover:text-gray-900">
          <i class="fas fa-user mr-2"></i>Cuenta
        </a>
        <a href="<%= settings_subscription_path %>" class="block px-3 py-2 rounded-md text-gray-700 hover:bg-gray-50 hover:text-gray-900">
          <i class="fas fa-credit-card mr-2"></i>Suscripción
        </a>
        <a href="<%= settings_notifications_path %>" class="block px-3 py-2 rounded-md text-gray-700 hover:bg-gray-50 hover:text-gray-900">
          <i class="fas fa-bell mr-2"></i>Notificaciones
        </a>
        <a href="<%= settings_security_path %>" class="block px-3 py-2 rounded-md text-gray-700 hover:bg-gray-50 hover:text-gray-900">
          <i class="fas fa-shield-alt mr-2"></i>Seguridad
        </a>
        <a href="<%= settings_language_path %>" class="block px-3 py-2 rounded-md bg-blue-100 text-blue-700 font-medium">
          <i class="fas fa-language mr-2"></i>Idioma
        </a>
      </nav>
    </div>
    
    <!-- Main Content -->
    <div class="md:col-span-3 bg-white rounded-lg shadow-md p-6">
      <h2 class="text-xl font-semibold mb-4">Preferencias de Idioma</h2>
      
      <%= form_with model: @user, url: settings_language_path, method: :patch, class: "space-y-6" do |f| %>
        <div>
          <%= f.label :language, "Selecciona tu idioma preferido", class: "block text-sm font-medium text-gray-700" %>
          <div class="mt-1">
            <%= f.select :language, 
                        options_for_select([
                          ["Español", "es"], 
                          ["English", "en"]
                        ], @user.language),
                        {}, 
                        { class: "mt-1 block w-full pl-3 pr-10 py-2 text-base border-gray-300 focus:outline-none focus:ring-primary-500 focus:border-primary-500 sm:text-sm rounded-md" } %>
          </div>
          <p class="mt-2 text-sm text-gray-500">Este idioma se utilizará en toda la plataforma.</p>
        </div>
        
        <div class="flex justify-end">
          <%= f.submit "Guardar Preferencias", class: "ml-3 inline-flex justify-center py-2 px-5 border border-transparent shadow-sm text-sm font-medium rounded-full text-white transition-all hover:shadow-md transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500", style: "background-color: #00bfff;" %>
        </div>
      <% end %>
      
      <div class="mt-8 pt-8 border-t border-gray-200">
        <h3 class="text-lg font-medium mb-4">Información sobre Idiomas</h3>
        
        <div class="bg-gray-50 p-4 rounded-md">
          <p class="text-sm text-gray-600">
            Actualmente, Doctores Honduras está disponible en español e inglés. Estamos trabajando para añadir más idiomas en el futuro.
          </p>
        </div>
      </div>
    </div>
  </div>
</div>
