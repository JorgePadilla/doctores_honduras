<div class="container mx-auto px-4 py-8 max-w-4xl">
  <div class="text-center mb-10">
    <h1 class="text-3xl font-bold text-gray-800 mb-2">Bienvenido a Doctores Honduras</h1>
    <p class="text-lg text-gray-600">Seleccione el tipo de perfil que desea crear</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8 items-stretch">
    <!-- Doctor -->
    <%= form_with url: onboarding_save_profile_type_path, method: :post, class: "block h-full" do |f| %>
      <%= f.hidden_field :profile_type, value: "doctor" %>
      <button type="submit" class="w-full h-full text-left bg-white rounded-lg shadow-lg overflow-hidden border-2 border-gray-200 hover:border-sky-500 transition-all cursor-pointer group">
        <div class="p-6">
          <div class="flex items-center mb-4">
            <div class="w-12 h-12 rounded-full bg-sky-100 flex items-center justify-center group-hover:bg-sky-200 transition-colors shrink-0">
              <svg class="w-6 h-6 text-sky-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-800 ml-4">Doctor</h3>
          </div>
          <p class="text-gray-600 text-sm">Para médicos y especialistas que desean crear su perfil profesional y aparecer en el directorio.</p>
        </div>
      </button>
    <% end %>

    <!-- Hospital / Clinic -->
    <%= form_with url: onboarding_save_profile_type_path, method: :post, class: "block h-full" do |f| %>
      <%= f.hidden_field :profile_type, value: "hospital" %>
      <button type="submit" class="w-full h-full text-left bg-white rounded-lg shadow-lg overflow-hidden border-2 border-gray-200 hover:border-green-500 transition-all cursor-pointer group">
        <div class="p-6">
          <div class="flex items-center mb-4">
            <div class="w-12 h-12 rounded-full bg-green-100 flex items-center justify-center group-hover:bg-green-200 transition-colors shrink-0">
              <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-800 ml-4">Hospital / Cl&iacute;nica</h3>
          </div>
          <p class="text-gray-600 text-sm">Para hospitales y cl&iacute;nicas que desean registrar su establecimiento y servicios.</p>
        </div>
      </button>
    <% end %>

    <!-- Vendor -->
    <%= form_with url: onboarding_save_profile_type_path, method: :post, class: "block h-full" do |f| %>
      <%= f.hidden_field :profile_type, value: "vendor" %>
      <button type="submit" class="w-full h-full text-left bg-white rounded-lg shadow-lg overflow-hidden border-2 border-gray-200 hover:border-purple-500 transition-all cursor-pointer group">
        <div class="p-6">
          <div class="flex items-center mb-4">
            <div class="w-12 h-12 rounded-full bg-purple-100 flex items-center justify-center group-hover:bg-purple-200 transition-colors shrink-0">
              <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-800 ml-4">Proveedor M&eacute;dico</h3>
          </div>
          <p class="text-gray-600 text-sm">Para proveedores de equipos y suministros m&eacute;dicos que desean mostrar sus productos.</p>
        </div>
      </button>
    <% end %>
  </div>

  <!-- Viewer link -->
  <div class="text-center">
    <%= form_with url: onboarding_save_profile_type_path, method: :post, class: "inline" do |f| %>
      <%= f.hidden_field :profile_type, value: "viewer" %>
      <button type="submit" class="text-sky-600 hover:text-sky-800 text-sm underline">
        Solo buscar doctores y establecimientos
      </button>
    <% end %>
  </div>
</div>
