<div class="container mx-auto px-4 py-8 max-w-3xl">
  <h1 class="text-2xl font-bold text-primary-700 dark:text-sky-400 mb-6">Nueva Cita</h1>

  <% if @branches.any? %>
    <%= render "form", appointment: @appointment, branches: @branches, form_url: agenda_appointments_path %>
  <% else %>
    <div class="bg-amber-50 dark:bg-amber-900/20 border border-amber-200 dark:border-amber-800 rounded-xl p-6 text-center">
      <svg class="w-12 h-12 text-amber-500 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <h3 class="text-lg font-semibold text-amber-800 dark:text-amber-300 mb-2">No tienes sucursales configuradas</h3>
      <p class="text-amber-700 dark:text-amber-400 text-sm mb-4">Agrega al menos una sucursal con horarios para poder crear citas.</p>
      <%= link_to "Configurar sucursales", edit_profile_path, class: "inline-flex items-center bg-sky-500 hover:bg-sky-600 text-white px-5 py-2.5 rounded-full font-medium transition-all shadow-md hover:shadow-lg transform hover:scale-105" %>
    </div>
  <% end %>
</div>
